# GoCalma — Python 3.9+

# UI
streamlit>=1.30.0,<2.0

# PDF parsing and rendering
pymupdf>=1.23.0,<2.0

# Encryption (audit log, redacted PDF protection)
cryptography>=42.0.0,<45.0

# Image processing (used by Surya OCR renderer)
Pillow>=10.0.0,<11.0

# OCR — transformer-based, language-agnostic, no system binary needed
surya-ocr>=0.4.0,<0.5

# NER model + LLM inference (BERT + Qwen2.5-0.5B-Instruct)
transformers>=4.37.0,<5.0
torch>=2.1.0,<3.0
huggingface_hub>=0.20.0,<1.0

# Optional: Tesseract fallback OCR
# Requires system binary: brew install tesseract tesseract-lang  (macOS)
#                         apt-get install tesseract-ocr tesseract-ocr-deu ...  (Linux)
# pytesseract>=0.3.10,<1.0
